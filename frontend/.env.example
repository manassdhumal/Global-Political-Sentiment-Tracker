# Base URL of the FastAPI backend.
# Local dev defaults to http://localhost:8000 if unset.
# On Vercel, set this (Project → Settings → Environment Variables) to your
# deployed API URL BEFORE building — NEXT_PUBLIC_* vars are inlined at build time.
NEXT_PUBLIC_API_BASE=https://your-api.onrender.com
